<p id="notice"><%= notice %></p>

<h1>Toy City Products</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Price</th>
      <th>Stock</th>
      <th>Brand</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.title %></td>
        <td><%= product.price %></td>
        <td><%= product.stock %></td>
        <td><%= @brand %></td>
        <td><%= link_to 'Show', product %></td>
        <td><%= link_to 'Edit', edit_product_path(product) %></td>
        <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Product', new_product_path, class: "button radius" %>

<div class="row">
  <% @products.each_with_index do |product| %>
    <div class="small-4 columns">
      <div class="item-wrapper">
        <div class="img-wrapper">
          <a class="button expand add-to-cart">Add to Cart</a>
          <a href="#"><%= image_tag product.image.url %></a>
        </div>
        <a href="#"><h3><%= product.title %></h3></a>
        <h5>$<%= product.price %></h5>
        <p><%= product.description %></p>
      </div>
    </div>
    <% end %>
  </div>
